services:
  - type: web
    name: seongdong-marketing-helper
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app_minimal:app --bind 0.0.0.0:$PORT --workers 1 --threads 1 --timeout 30 --graceful-timeout 15 --max-requests 20 --max-requests-jitter 5 --access-logfile - --error-logfile - --log-level warning
    healthCheckPath: /health
    envVars:
      - key: GOOGLE_API_KEY
        sync: false
      - key: FLASK_ENV
        value: production
